import 'package:flutter/material.dart';

/// Placeholder — full implementation in Phase 5.1
class BuddyDiscoveryScreen extends StatelessWidget {
  const BuddyDiscoveryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Buddy Discovery')),
    );
  }
}
